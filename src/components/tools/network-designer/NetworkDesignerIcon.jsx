import React from 'react';
import { IconNetwork } from '@tabler/icons-react';

const NetworkDesignerIcon = ({ size = 24, ...props }) => (
  <IconNetwork size={size} {...props} />
);

export default NetworkDesignerIcon;


