import React from 'react';
import { IconDeviceIpadSearch } from '@tabler/icons-react';

const WHOISIcon = ({ size = 24, ...props }) => (
  <IconDeviceIpadSearch size={size} {...props} />
);

export default WHOISIcon;