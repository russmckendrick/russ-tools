import React from 'react';

const CronFieldSelector = ({ field, value, onChange }) => {
  // TODO: Implement dropdowns or inputs for each cron field
  return <div>{field} selector (coming soon)</div>;
};

export default CronFieldSelector;
