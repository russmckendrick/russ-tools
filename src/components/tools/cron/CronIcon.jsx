import React from 'react';
import { IconClockFilled } from '@tabler/icons-react';

const CronIcon = ({ size = 24, ...props }) => (
  <IconClockFilled size={size} {...props} />
);

export default CronIcon;
