import React from 'react';
import { IconLink } from '@tabler/icons-react';

const MicrosoftPortalsIcon = ({ size = 24, ...props }) => (
  <IconLink size={size} {...props} />
);

export default MicrosoftPortalsIcon;