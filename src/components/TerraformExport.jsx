import React from 'react';

// Placeholder UI for Terraform export modal/dialog
export default function TerraformExport() {
  return (
    <div>
      <h2>Export as Terraform</h2>
      <p>Select a provider to generate Terraform code for your network design.</p>
      {/* TODO: Add tabbed interface and code blocks for AWS/Azure */}
    </div>
  );
}
